apply plugin: "war"

bootWar {
    mainClassName = "com.zacfrostart.main.ZacFrostArtApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
